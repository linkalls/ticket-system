<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>呼び出し済みチケット一覧</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://unpkg.com/htmx.org@1.6.1"></script>
    <style>
        .ticket-number {
            font-size: 5rem; /* 文字サイズを大きく */
            font-weight: bold;
            margin: 1rem 0;
            text-align: center;
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto mt-5 px-4">
        <h1 class="text-2xl font-bold text-center mb-5">呼び出し済みチケット一覧</h1>
        <div class="bg-white p-5 rounded shadow-md">
            <div id="calledTickets" hx-get="/api/tickets/called" hx-trigger="every 1s" hx-swap="innerHTML">
                <!-- チケット番号がここに表示されます -->
            </div>
        </div>
    </div>
    <script>
      document.addEventListener('htmx:afterSwap', function(event) {
          if (event.detail.target.id === 'calledTickets') {
              const ticketNumbers = JSON.parse(event.detail.xhr.responseText);
              const calledTicketsDiv = document.getElementById('calledTickets');
              calledTicketsDiv.innerHTML = ticketNumbers.map(number => `<div class="ticket-number">${number}</div>`).join('');
          }
      });
  </script>
</body>
</html>